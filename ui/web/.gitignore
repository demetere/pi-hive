node_modules/
*.log
# dist/ is intentionally committed — it is the prebuilt dashboard the
# telemetry server ships and serves. Rebuild from the repository root with
# `just build-dashboard` after changing anything under src/.
