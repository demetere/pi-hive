name: Parent Chat
description: Identify the parent fixture from its nearest manifest.
use-when: Testing nested project discovery.
tags: [fixture]

artifact:
  adapter: none
  profile: default
  binding: none
  options: {}

team:
  id: root
  agent: parent-root

instructions:
  root: |
    Identify the parent fixture.
